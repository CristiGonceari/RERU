export interface Sidebar {
	SIDEBAR: number;
	MODULES: number;
	USER: number;
}

export enum SidebarView {
	SIDEBAR = 1,
	MODULES = 2,
	USER = 3,
}

export enum SidebarItemType {
  SECTION = 1,
  ITEM = 2
}
