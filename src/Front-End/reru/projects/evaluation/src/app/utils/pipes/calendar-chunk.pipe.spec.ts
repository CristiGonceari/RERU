import { CalendarChunkPipe } from './calendar-chunk.pipe';

describe('CalendarChunkPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarChunkPipe();
    expect(pipe).toBeTruthy();
  });
});
