/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PollsByEventComponent } from './polls-by-event.component';

describe('PollsByEventComponent', () => {
  let component: PollsByEventComponent;
  let fixture: ComponentFixture<PollsByEventComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PollsByEventComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PollsByEventComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/